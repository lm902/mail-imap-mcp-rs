name: IMAP Integration Tests

on:
  workflow_dispatch:

jobs:
  greenmail-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run GreenMail integration tests
        run: bash scripts/test-greenmail.sh

      - name: Run MCP inspector integration tests
        run: bash scripts/test-greenmail-mcp-inspector.sh
